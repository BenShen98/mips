#include <iostream>

int main ()
{
  std::cerr<<"Enter text. Include a dot ('.') in a sentence to exit:"<<std::endl;
  std::cout<<"2"<<std::flush;
  // do {
  //   c=getchar();
  //   putchar (c);
  // } while (c != '.');
  return 0;
}
